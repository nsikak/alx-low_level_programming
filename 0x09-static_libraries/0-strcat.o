/**
 * _strcat - strcat
 *Description: just a func
 *Return: char
 *@dest: dest
 *@src: src
 */

char *_strcat(char *dest, char *src)
{
	char *start = dest;

	while (*dest)
	{
		dest++;
	}

	while (*src)
	{
		*dest = *src;
		dest++;
		src++;
	}

	return (start);
}
