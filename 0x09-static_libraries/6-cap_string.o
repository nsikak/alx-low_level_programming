/**
 * cap_string - strcat
 *Description: just a func
 *Return: char
 *@s: src
 */

char *cap_string(char *s);
{
	int i = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
		if (s[i] >= 'a' && s[i] <= 'z')
			s[i] += 32;
	}
	return (s);

}
