#include "main.h"

/**
 * print_rev - resets a pointer
 * Description: Just a function
 * Return: 0
 * @s: pointer params
 */

void print_rev(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	while (i >= 0)
	{
		_putchar(s[i]);
		i--;
	}
}
